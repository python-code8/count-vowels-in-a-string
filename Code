# Program to count the no.of vowels

vowels = 'aeiou' 
ip_str = 'Like, share, save'
ipstr =  ip_str.casefold()

# make a dictionary

count = {}.fromkeys(vowels, 0)

# count the vowels

for char in ip_str:
    if char in count:
        count[char] += 1
print(count)
